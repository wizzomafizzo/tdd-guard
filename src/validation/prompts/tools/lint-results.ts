export const LINT_RESULTS = `## Code Quality

Static analysis results from the current codebase BEFORE this modification.

**Priority**: In red phase (failing tests), focus on making tests pass. In green phase (passing tests), address lint issues before new features.

### Lint Results
`
