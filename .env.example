# TDD Guard Configuration

# Path to Claude CLI binary
# Use this if Claude is installed in a non-standard location
# For local installations after /migrate-to-local:
# CLAUDE_BINARY_PATH=/Users/YOUR_USERNAME/.claude/local/claude
CLAUDE_BINARY_PATH=

# Directory where TDD Guard stores all data (test reports, logs, etc)
# Defaults to .claude/tdd-guard/data in the current working directory
TDD_DATA_DIR=

